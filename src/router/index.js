import { routes } from './routes';
import {
  addQuery,
  removeQuery,
  getCurrentLocation,
  createSearchParams
} from './utils';

export {
  routes,
  addQuery,
  removeQuery,
  getCurrentLocation,
  createSearchParams
}
