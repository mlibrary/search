import { routes } from './routes';
import {
  addQuery,
  removeQuery,
  getCurrentLocation,
  getSearchQueries
} from './utils';

export {
  routes,
  addQuery,
  removeQuery,
  getCurrentLocation,
  getSearchQueries
}
